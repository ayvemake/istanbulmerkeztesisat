<div class="bg-gray-50 rounded-xl p-6 space-y-6">
  <h4 class="font-semibold text-gray-900">Hizmet Avantajları</h4>
  <div class="grid gap-4">
    <% service.service_advantages.each do |advantage| %>
      <div class="flex items-start gap-3">
        <svg class="w-6 h-6 text-blue-800 flex-shrink-0 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
        <div>
          <h5 class="font-medium text-gray-900"><%= advantage.title %></h5>
          <p class="text-gray-600"><%= advantage.description %></p>
        </div>
      </div>
    <% end %>
  </div>
</div> 