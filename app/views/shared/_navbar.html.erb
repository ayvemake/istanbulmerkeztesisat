<!-- Navbar fixe -->
<nav class="bg-white shadow-lg fixed w-full top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Desktop Navigation -->
    <div class="flex justify-between h-16">
      <!-- Logo -->
      <div class="flex-shrink-0 flex items-center">
        <%= link_to root_path, class: "text-2xl font-bold text-gray-600" do %>
          İstanbul Merkez Tesisat
        <% end %>
      </div>

      <!-- Desktop Links -->
      <div class="hidden sm:flex sm:items-center sm:space-x-8">
        <%= link_to "Ana Sayfa", root_path, 
            class: "#{current_page?(root_path) ? 'text-gray-800 nav-link-active' : 'text-gray-600 hover:text-gray-800 nav-link'} 
                   px-3 h-16 flex items-center text-sm font-medium transition-all duration-200" %>
        
        <%= link_to "Hizmetlerimiz", services_path,
            class: "#{current_page?(services_path) ? 'text-gray-800 border-b-2 border-gray-800' : 'text-gray-600 hover:text-gray-800 border-b-2 border-transparent hover:border-gray-300'} 
                   px-3 py-2 text-sm font-medium transition-all duration-200" %>
        
        <%= link_to "Hakkımızda", about_path,
            class: "#{current_page?(about_path) ? 'text-gray-800 border-b-2 border-gray-800' : 'text-gray-600 hover:text-gray-800 border-b-2 border-transparent hover:border-gray-300'} 
                   px-3 py-2 text-sm font-medium transition-all duration-200" %>
        
        <%= link_to "İletişim", new_customer_inquiry_path,
            class: "#{current_page?(new_customer_inquiry_path) ? 'text-gray-800 border-b-2 border-gray-800' : 'text-gray-600 hover:text-gray-800 border-b-2 border-transparent hover:border-gray-300'} 
                   px-3 py-2 text-sm font-medium transition-all duration-200" %>
        
        <%= link_to "Teknik Bilgiler", technical_info_path,
            class: "#{current_page?(technical_info_path) ? 'text-gray-800 border-b-2 border-gray-800' : 'text-gray-600 hover:text-gray-800 border-b-2 border-transparent hover:border-gray-300'} 
                   px-3 py-2 text-sm font-medium transition-all duration-200" %>

        <!-- Desktop Call Button -->
        <a href="tel:+905555555555" class="ml-8 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-gray-600 hover:bg-gray-700">
          <svg class="h-4 w-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
          </svg>
          7/24 Acil Servis
        </a>
      </div>

      <!-- Mobile menu button -->
      <div class="sm:hidden flex items-center">
        <button type="button" class="mobile-menu-button inline-flex items-center justify-center p-2 rounded-md text-gray-700 hover:text-gray-600 hover:bg-gray-100">
          <span class="sr-only menu-button-text">Menüyü aç</span>
          <!-- Icône menu (hamburger) -->
          <svg class="menu-open-icon block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <!-- Icône fermer (X) - cachée par défaut -->
          <svg class="menu-close-icon hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>
</nav>

<!-- Spacer pour compenser la navbar fixe -->
<div class="h-16"></div>

<!-- Mobile menu -->
<div class="mobile-menu hidden sm:hidden fixed top-0 right-0 w-64 h-screen bg-white shadow-lg z-40 transform transition-transform duration-300 ease-in-out rounded-l-2xl border-l border-gray-200">
  <!-- Spacer pour le menu mobile -->
  <div class="h-16"></div>
  
  <div class="px-2 pt-2 pb-3 divide-y divide-gray-200">
    <%= link_to "Ana Sayfa", root_path, 
        class: "#{current_page?(root_path) ? 'bg-gray-50 text-gray-600' : 'text-gray-700'} 
               block px-3 py-3 text-base font-medium hover:bg-gray-50" %>
    
    <%= link_to "Hizmetlerimiz", services_path,
        class: "#{current_page?(services_path) ? 'bg-gray-50 text-gray-600' : 'text-gray-700'} 
               block px-3 py-3 text-base font-medium hover:bg-gray-50" %>
    
    <%= link_to "Hakkımızda", about_path,
        class: "#{current_page?(about_path) ? 'bg-gray-50 text-gray-600' : 'text-gray-700'} 
               block px-3 py-3 text-base font-medium hover:bg-gray-50" %>
    
    <%= link_to "İletişim", new_customer_inquiry_path,
        class: "#{current_page?(new_customer_inquiry_path) ? 'bg-gray-50 text-gray-600' : 'text-gray-700'} 
               block px-3 py-3 text-base font-medium hover:bg-gray-50" %>
    
    <%= link_to "Teknik Bilgiler", technical_info_path,
        class: "#{current_page?(technical_info_path) ? 'bg-gray-50 text-gray-600' : 'text-gray-700'} 
               block px-3 py-3 text-base font-medium hover:bg-gray-50" %>

    <!-- Mobile Call Button -->
    <div class="px-3 py-3">
      <a href="tel:+905555555555" class="flex items-center justify-center w-full px-4 py-2 border border-transparent text-base font-medium rounded-md text-white bg-gray-600 hover:bg-gray-700">
        <svg class="h-5 w-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
        </svg>
        7/24 Acil Servis
      </a>
    </div>
  </div>
</div>

<!-- Overlay pour le menu mobile -->
<div class="mobile-menu-overlay hidden sm:hidden fixed inset-0 bg-black/40 backdrop-blur-sm transition-all duration-300 z-30"></div>

<%= javascript_tag do %>
  document.addEventListener('DOMContentLoaded', function() {
    const btn = document.querySelector('.mobile-menu-button');
    const menu = document.querySelector('.mobile-menu');
    const overlay = document.querySelector('.mobile-menu-overlay');
    const menuOpenIcon = document.querySelector('.menu-open-icon');
    const menuCloseIcon = document.querySelector('.menu-close-icon');
    const menuButtonText = document.querySelector('.menu-button-text');

    btn.addEventListener('click', () => {
      menu.classList.toggle('hidden');
      overlay.classList.toggle('hidden');
      document.body.classList.toggle('overflow-hidden');
      
      // Changer l'icône et le texte
      menuOpenIcon.classList.toggle('hidden');
      menuCloseIcon.classList.toggle('hidden');
      menuButtonText.textContent = menuOpenIcon.classList.contains('hidden') ? 'Menüyü kapat' : 'Menüyü aç';
      
      // Animation du menu
      if (!menu.classList.contains('hidden')) {
        menu.classList.add('translate-x-0');
        menu.classList.remove('translate-x-full');
        overlay.classList.add('opacity-100');
        overlay.classList.remove('opacity-0');
      } else {
        menu.classList.remove('translate-x-0');
        menu.classList.add('translate-x-full');
        overlay.classList.remove('opacity-100');
        overlay.classList.add('opacity-0');
      }
    });

    // Fermer le menu en cliquant sur l'overlay
    overlay.addEventListener('click', () => {
      menu.classList.add('hidden', 'translate-x-full');
      menu.classList.remove('translate-x-0');
      overlay.classList.add('hidden', 'opacity-0');
      overlay.classList.remove('opacity-100');
      document.body.classList.remove('overflow-hidden');
      
      // Remettre l'icône et le texte initial
      menuOpenIcon.classList.remove('hidden');
      menuCloseIcon.classList.add('hidden');
      menuButtonText.textContent = 'Menüyü aç';
    });
  });
<% end %> 