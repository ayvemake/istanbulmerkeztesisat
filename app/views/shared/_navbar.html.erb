<nav class="bg-white shadow-lg" data-controller="mobile-menu">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <!-- Logo et Navigation principale -->
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_path, class: "text-2xl font-bold text-emerald-600" do %>
            İstanbul Merkez Tesisat
          <% end %>
        </div>

        <!-- Navigation principale -->
        <div class="hidden sm:ml-8 sm:flex sm:space-x-8">
          <%= link_to "Ana Sayfa", root_path, 
              class: "#{current_page?(root_path) ? 'border-emerald-500 text-emerald-600' : 'border-transparent text-gray-700 hover:text-emerald-600'} 
                     inline-flex items-center px-1 pt-1 border-b-2 text-lg font-bold transition-colors" %>
          
          <%= link_to "Hizmetlerimiz", services_path,
              class: "#{current_page?(services_path) ? 'border-emerald-500 text-emerald-600' : 'border-transparent text-gray-700 hover:text-emerald-600'} 
                     inline-flex items-center px-1 pt-1 border-b-2 text-lg font-bold transition-colors" %>
          
          <%= link_to "Hakkımızda", about_path,
              class: "#{current_page?(about_path) ? 'border-emerald-500 text-emerald-600' : 'border-transparent text-gray-700 hover:text-emerald-600'} 
                     inline-flex items-center px-1 pt-1 border-b-2 text-lg font-bold transition-colors" %>
          
          <%= link_to "İletişim", new_customer_inquiry_path,
              class: "#{current_page?(new_customer_inquiry_path) ? 'border-emerald-500 text-emerald-600' : 'border-transparent text-gray-700 hover:text-emerald-600'} 
                     inline-flex items-center px-1 pt-1 border-b-2 text-lg font-bold transition-colors" %>
          
          <%= link_to "Teknik Bilgiler", technical_info_path,
              class: "#{current_page?(technical_info_path) ? 'border-emerald-500 text-emerald-600' : 'border-transparent text-gray-700 hover:text-emerald-600'} 
                     inline-flex items-center px-1 pt-1 border-b-2 text-lg font-bold transition-colors" %>
        </div>
      </div>

      <!-- Bouton d'appel -->
      <div class="hidden sm:flex sm:items-center">
        <a href="tel:+905555555555" class="inline-flex items-center px-6 py-3 border border-transparent text-lg font-bold rounded-md text-white bg-emerald-600 hover:bg-emerald-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-emerald-500">
          <svg class="h-5 w-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
          </svg>
          7/24 Acil Servis
        </a>
      </div>

      <!-- Bouton menu mobile -->
      <button type="button" 
              data-action="mobile-menu#toggle"
              class="sm:hidden inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-gray-500">
        <span class="sr-only">Menu</span>
        <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
      </button>
    </div>
  </div>

  <!-- Menu mobile -->
  <div class="fixed inset-y-0 right-0 w-64 bg-white shadow-xl transform transition-transform duration-300 ease-in-out translate-x-full z-50"
       data-mobile-menu-target="menu">
    <div class="h-full flex flex-col">
      <div class="p-4 bg-emerald-600 flex justify-between items-center">
        <span class="text-white text-lg font-bold">Menü</span>
        <button type="button" 
                data-action="mobile-menu#toggle"
                class="p-2 rounded-md text-white hover:bg-emerald-700">
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>

      <div class="flex-1 overflow-y-auto">
        <div class="py-2">
          <%= link_to "Ana Sayfa", root_path, 
              class: "#{current_page?(root_path) ? 'bg-emerald-50 text-emerald-700' : 'text-gray-600'} 
                     block px-4 py-3 text-lg font-bold hover:bg-gray-50",
              data: { action: "mobile-menu#toggle" } %>
          
          <%= link_to "Hizmetlerimiz", services_path,
              class: "#{current_page?(services_path) ? 'bg-emerald-50 text-emerald-700' : 'text-gray-600'} 
                     block px-4 py-3 text-lg font-bold hover:bg-gray-50",
              data: { action: "mobile-menu#toggle" } %>
          
          <%= link_to "Hakkımızda", about_path,
              class: "#{current_page?(about_path) ? 'bg-emerald-50 text-emerald-700' : 'text-gray-600'} 
                     block px-4 py-3 text-lg font-bold hover:bg-gray-50",
              data: { action: "mobile-menu#toggle" } %>
          
          <%= link_to "İletişim", new_customer_inquiry_path,
              class: "#{current_page?(new_customer_inquiry_path) ? 'bg-emerald-50 text-emerald-700' : 'text-gray-600'} 
                     block px-4 py-3 text-lg font-bold hover:bg-gray-50",
              data: { action: "mobile-menu#toggle" } %>
          
          <%= link_to "Teknik Bilgiler", technical_info_path,
              class: "#{current_page?(technical_info_path) ? 'bg-emerald-50 text-emerald-700' : 'text-gray-600'} 
                     block px-4 py-3 text-lg font-bold hover:bg-gray-50",
              data: { action: "mobile-menu#toggle" } %>
        </div>
      </div>
    </div>
  </div>

  <!-- Overlay -->
  <div class="fixed inset-0 bg-black bg-opacity-50 transition-opacity duration-300 opacity-0 pointer-events-none z-40"
       data-mobile-menu-target="overlay"
       data-action="click->mobile-menu#toggle">
  </div>
</nav> 